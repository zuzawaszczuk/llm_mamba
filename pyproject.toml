[project]
name = "llm-mamba"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "datasets>=4.8.5",
    "torch>=2.12.0",
    "transformers>=5.8.1",
]
